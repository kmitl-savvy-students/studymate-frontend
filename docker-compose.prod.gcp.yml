services:
    frontend:
        container_name: studymate-nginx
        image: frontend-prod-img
        restart: always
        volumes:
            - ./nginx.prod.gcp.conf:/etc/nginx/conf.d/default.conf
        ports:
            - 80:80

